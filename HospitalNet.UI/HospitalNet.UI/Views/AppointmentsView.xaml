<UserControl x:Class="HospitalNet.UI.Views.AppointmentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="{StaticResource BackgroundBrush}">
    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Vertical" Margin="20" Background="Transparent">
            <TextBlock Text="Appointment Scheduler" FontSize="{StaticResource FontSizeXLarge}" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,10"/>
            <TextBlock Text="Schedule appointments with automatic double-booking prevention" FontSize="{StaticResource FontSizeNormal}" Foreground="{StaticResource TextSecondaryBrush}"/>
        </StackPanel>

        <Border Grid.Row="1" Margin="20,0,20,20" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="250"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Doctor:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                <ComboBox Grid.Column="1" x:Name="DoctorComboBox" Height="35" VerticalAlignment="Center" SelectionChanged="DoctorComboBox_SelectionChanged" DisplayMemberPath="FullName" SelectedValuePath="DoctorID"/>

                <TextBlock Grid.Column="2" Text="Date:" VerticalAlignment="Center" Margin="20,0,10,0" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                <DatePicker Grid.Column="3" x:Name="DatePicker" Height="35" VerticalAlignment="Center" SelectedDateChanged="DatePicker_SelectedDateChanged"/>
            </Grid>
        </Border>

        <Border Grid.Row="2" Margin="20,0,20,20" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="320"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.ColumnSpan="3" Text="Appointments for Selected Doctor &amp; Date" FontSize="{StaticResource FontSizeMedium}" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,10"/>

                <Border Grid.Row="1" Grid.Column="0" Background="{StaticResource BackgroundBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="10">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Header -->
                        <Grid Grid.Row="0" Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="120"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Time" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                            <TextBlock Grid.Column="1" Text="Patient" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                            <TextBlock Grid.Column="2" Text="Reason" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                            <TextBlock Grid.Column="3" Text="Status" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                        </Grid>

                        <ListBox Grid.Row="1"
                                 x:Name="AppointmentsListBox"
                                 SelectionChanged="AppointmentsListBox_SelectionChanged"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 ScrollViewer.VerticalScrollBarVisibility="Auto">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{StaticResource SurfaceBrush}"
                                            BorderBrush="{StaticResource BorderBrush}"
                                            BorderThickness="1"
                                            CornerRadius="4"
                                            Padding="10"
                                            Margin="0,0,0,8">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="80"/>
                                                <ColumnDefinition Width="200"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="120"/>
                                            </Grid.ColumnDefinitions>

                                            <TextBlock Grid.Column="0" Text="{Binding AppointmentTime, StringFormat={}{0:HH:mm}}" Foreground="{StaticResource TextPrimaryBrush}" />
                                            <TextBlock Grid.Column="1" Text="{Binding PatientName}" Foreground="{StaticResource TextPrimaryBrush}" TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Grid.Column="2" Text="{Binding ReasonForVisit}" Foreground="{StaticResource TextPrimaryBrush}" TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Grid.Column="3" Text="{Binding Status}" Foreground="{StaticResource TextPrimaryBrush}" TextTrimming="CharacterEllipsis"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>

                <!-- Selected items -->
                <Border Grid.Row="1" Grid.Column="2" Background="{StaticResource BackgroundBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4" Margin="0,0,0,0" Padding="12">
                    <StackPanel>
                        <TextBlock Text="Selected Details" FontSize="{StaticResource FontSizeMedium}" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,10"/>

                        <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,0,0,10">
                            <StackPanel>
                                <TextBlock Text="Doctor" FontSize="{StaticResource FontSizeSmall}" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBlock x:Name="SelectedDoctorText" Text="-" Foreground="{StaticResource TextPrimaryBrush}" FontWeight="Bold"/>
                            </StackPanel>
                        </Border>

                        <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,0,0,10">
                            <StackPanel>
                                <TextBlock Text="Patient" FontSize="{StaticResource FontSizeSmall}" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBlock x:Name="SelectedPatientText" Text="-" Foreground="{StaticResource TextPrimaryBrush}" FontWeight="Bold"/>
                            </StackPanel>
                        </Border>

                        <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="10">
                            <StackPanel>
                                <TextBlock Text="Appointment" FontSize="{StaticResource FontSizeSmall}" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBlock x:Name="SelectedAppointmentText" Text="-" Foreground="{StaticResource TextPrimaryBrush}" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                            <Button x:Name="CompleteAppointmentButton" Width="120" Height="32" Style="{StaticResource PrimaryButtonStyle}" Click="CompleteAppointmentButton_Click" IsEnabled="False">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE930;" FontFamily="Segoe MDL2 Assets" Margin="0,0,6,0"/>
                                    <TextBlock Text="Complete"/>
                                </StackPanel>
                            </Button>
                            <Button x:Name="CancelAppointmentButton" Width="120" Height="32" Margin="10,0,0,0" Style="{StaticResource SecondaryButtonStyle}" Click="CancelAppointmentButton_Click" IsEnabled="False">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE711;" FontFamily="Segoe MDL2 Assets" Margin="0,0,6,0"/>
                                    <TextBlock Text="Cancel"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <Border Grid.Row="3" Margin="20,0,20,20" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Patient:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                <ComboBox Grid.Column="1" x:Name="PatientComboBox" Height="35" VerticalAlignment="Center" DisplayMemberPath="FullName" SelectedValuePath="PatientID" SelectionChanged="PatientComboBox_SelectionChanged"/>

                <TextBlock Grid.Column="2" Text="Time:" VerticalAlignment="Center" Margin="20,0,10,0" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                <TextBox Grid.Column="3" x:Name="TimeTextBox" Height="35" VerticalAlignment="Center" Padding="10,0,10,0" VerticalContentAlignment="Center" Style="{StaticResource StandardTextBoxStyle}" Text="09:00" Foreground="{StaticResource TextPrimaryBrush}"/>

                <TextBlock Grid.Column="4" Text="Reason:" VerticalAlignment="Center" Margin="20,0,10,0" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                <TextBox Grid.Column="5" x:Name="ReasonTextBox" Height="35" VerticalAlignment="Center" Padding="10,0,10,0" VerticalContentAlignment="Center" Style="{StaticResource StandardTextBoxStyle}" Text="" Foreground="{StaticResource TextPrimaryBrush}"/>

                <StackPanel Grid.Column="6" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button x:Name="BookButton" Content="Book Appointment" Width="180" Height="35" Style="{StaticResource PrimaryButtonStyle}" Click="BookButton_Click" Cursor="Hand" Margin="10,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
