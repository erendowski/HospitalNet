<UserControl x:Class="HospitalNet.UI.Views.AnalyticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="{StaticResource BackgroundBrush}">
    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Vertical" Margin="20" Background="Transparent">
            <TextBlock Text="Analytics &amp; Performance Metrics" FontSize="{StaticResource FontSizeXLarge}" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,10"/>
            <TextBlock Text="Hospital performance and utilization metrics" FontSize="{StaticResource FontSizeNormal}" Foreground="{StaticResource TextSecondaryBrush}"/>
        </StackPanel>

        <Border Grid.Row="1" Margin="20,0,20,20" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Start Date:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                <DatePicker Grid.Column="1" x:Name="StartDatePicker" Height="35" VerticalAlignment="Center"/>

                <TextBlock Grid.Column="2" Text="End Date:" VerticalAlignment="Center" Margin="20,0,10,0" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                <DatePicker Grid.Column="3" x:Name="EndDatePicker" Height="35" VerticalAlignment="Center"/>

                <Button Grid.Column="5" x:Name="GenerateReportButton" Width="160" Height="35" Style="{StaticResource PrimaryButtonStyle}" Click="GenerateReportButton_Click" Cursor="Hand">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="&#xE9F9;" FontFamily="Segoe MDL2 Assets" FontSize="14" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBlock Text="Generate Report" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button Grid.Column="6" x:Name="GenerateAiSummaryButton" Width="160" Height="35" Style="{StaticResource SecondaryButtonStyle}" Click="GenerateAiSummaryButton_Click" Cursor="Hand" Margin="10,0,0,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="&#xE946;" FontFamily="Segoe MDL2 Assets" FontSize="14" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBlock Text="AI Summary" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="20,0,20,20" Height="120">
            <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="20" Margin="0,0,15,0" Width="280">
                <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                    <TextBlock Text="Total Appointments" FontSize="{StaticResource FontSizeSmall}" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,10"/>
                    <TextBlock x:Name="TotalAppointmentsMetric" Text="0" FontSize="36" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}"/>
                </StackPanel>
            </Border>

            <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="20" Margin="0,0,15,0" Width="280">
                <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                    <TextBlock Text="Completed Visits" FontSize="{StaticResource FontSizeSmall}" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,10"/>
                    <TextBlock x:Name="CompletedVisitsMetric" Text="0" FontSize="36" FontWeight="Bold" Foreground="{StaticResource SecondaryBrush}"/>
                </StackPanel>
            </Border>

            <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="20" Margin="0,0,15,0" Width="280">
                <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                    <TextBlock Text="Cancellation Rate" FontSize="{StaticResource FontSizeSmall}" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,10"/>
                    <TextBlock x:Name="CancellationRateMetric" Text="0%" FontSize="36" FontWeight="Bold" Foreground="{StaticResource DangerBrush}"/>
                </StackPanel>
            </Border>

            <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="20" Margin="0,0,0,0" Width="280">
                <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                    <TextBlock Text="Total Patients" FontSize="{StaticResource FontSizeSmall}" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,10"/>
                    <TextBlock x:Name="PatientLoadMetric" Text="0" FontSize="36" FontWeight="Bold" Foreground="{StaticResource InfoBrush}"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <Border Grid.Row="3" Margin="20,0,20,20" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="20">
            <StackPanel>
                <TextBlock Text="AI Summary" FontSize="{StaticResource FontSizeMedium}" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,8"/>
                <TextBox x:Name="AiSummaryTextBox" Height="220" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" IsReadOnly="True" Style="{StaticResource StandardTextBoxStyle}" Background="{StaticResource BackgroundBrush}" />
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="20" Height="30" VerticalAlignment="Center">
            <TextBlock x:Name="StatusTextBlock" Text="Ready to generate report" Foreground="{StaticResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeSmall}" VerticalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
