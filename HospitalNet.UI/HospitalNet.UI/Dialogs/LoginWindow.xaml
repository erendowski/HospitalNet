<Window x:Class="HospitalNet.UI.Dialogs.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="420" Width="520" MinHeight="380"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="HospitalNet Login" FontSize="{StaticResource FontSizeXLarge}" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>

        <Grid Grid.Row="1" Margin="0,12,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Margin="0,0,0,12" Text="Sign in to continue." FontSize="{StaticResource FontSizeNormal}" Foreground="{StaticResource TextSecondaryBrush}"/>

            <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="14">
                <StackPanel>
                    <TextBlock Text="Authentication" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,10"/>

                    <CheckBox x:Name="UseWindowsAuthCheckBox" Content="Use Windows Authentication" Foreground="{StaticResource TextPrimaryBrush}" Checked="UseWindowsAuthCheckBox_Checked" Unchecked="UseWindowsAuthCheckBox_Checked"/>

                    <Grid Margin="0,12,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Username" VerticalAlignment="Center" Foreground="{StaticResource TextPrimaryBrush}"/>
                        <TextBox Grid.Row="0" Grid.Column="1" x:Name="UsernameTextBox" Height="32" Padding="10,0" VerticalContentAlignment="Center" Style="{StaticResource StandardTextBoxStyle}"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Password" Margin="0,10,0,0" VerticalAlignment="Center" Foreground="{StaticResource TextPrimaryBrush}"/>
                        <PasswordBox Grid.Row="1" Grid.Column="1" x:Name="PasswordBox" Height="32" Margin="0,10,0,0" Padding="10,0"/>
                    </Grid>

                    <TextBlock x:Name="ProfileTextBlock" Margin="0,12,0,0" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBlock x:Name="InfoTextBlock" Margin="0,8,0,0" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap"/>
                    <TextBlock x:Name="StatusTextBlock" Margin="0,10,0,0" Foreground="{StaticResource DangerBrush}" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Width="100" Height="34" Style="{StaticResource SecondaryButtonStyle}" Click="Cancel_Click">Cancel</Button>
            <Button Width="100" Height="34" Margin="10,0,0,0" Style="{StaticResource PrimaryButtonStyle}" Click="Login_Click">Login</Button>
        </StackPanel>
    </Grid>
</Window>
